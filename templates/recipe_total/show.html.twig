{% extends 'base.html.twig' %}

{% block title %}RecipeTotal{% endblock %}

{% block body %}
    <h1>RecipeTotal</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ recipe_total.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('recipe_total_index') }}">back to list</a>

    <a href="{{ path('recipe_total_edit', {'id': recipe_total.id}) }}">edit</a>

    {{ include('recipe_total/_delete_form.html.twig') }}
{% endblock %}
