{% extends 'base.html.twig' %}

{% block title %}New RecipeTotal{% endblock %}

{% block body %}
    <h1>Create new RecipeTotal</h1>

    {{ include('recipe_total/_form.html.twig') }}

    <a href="{{ path('recipe_total_index') }}">back to list</a>
{% endblock %}
